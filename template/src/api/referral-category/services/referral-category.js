'use strict';

/**
 * referral-category service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::referral-category.referral-category');
